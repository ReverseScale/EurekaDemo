# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EurekaDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Eureka', '~> 4.0.1'

  # Pods for EurekaDemo

  target 'EurekaDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EurekaDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
